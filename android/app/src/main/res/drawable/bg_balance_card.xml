<?xml version="1.0" encoding="utf-8"?>
<!-- The balance card: the entropy field's ink, the same in both themes (design/tokens.json `field`). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/field_ink" />
    <stroke android:width="1dp" android:color="@color/field_edge" />
    <corners android:radius="16dp" />
</shape>
