// The wallet in a full tab — the options page, the install-time welcome, and anywhere a popup is
// too small. The same screens as the popup; only the window differs.
import { boot } from './lib/boot.js';

boot('app');
